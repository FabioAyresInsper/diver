# pylint: disable=missing-docstring
from setuptools import setup, find_packages

setup(
    name='diver',
    version='0.1',
    packages=find_packages(),
    description='DIVeR: Deterministic Integration for Volume Rendering',
)